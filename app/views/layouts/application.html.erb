<!DOCTYPE html>
  <!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7" lang=""> <![endif]-->
  <!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8" lang=""> <![endif]-->
  <!--[if IE 8]>         <html class="no-js lt-ie9" lang=""> <![endif]-->
  <!--[if gt IE 8]><!--> <html class="no-js" lang=""> <!--<![endif]-->
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>Home :: Event Maker</title>

    <meta name="description" content="Event Maker">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    
    <%= favicon_link_tag 'favicon.png' %>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_include_tag 'vendor/modernizr-2.8.3-respond-1.4.2.min' %>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
  </head>

  <body>

    <div class='thetop'></div>

    <%= yield %>

    <div class="pre-loader"></div>

    <button type="button" class='scroll-top no-outline'>
      <i class="fa fa-angle-up"></i>
    </button>

    <script type="text/javascript">
      $(window).load(function() {
        $(".pre-loader").fadeOut(500);
      });

      $(window).scroll(function() {
        if ($(this).scrollTop() > 50 ) {
          $('.scroll-top').stop(true, true).fadeIn();
        } else {
          $('.scroll-top').stop(true, true).fadeOut();
        }
      });

      $(function(){$(".scroll-top").click(function(){
        $("html,body").animate({scrollTop:$(".thetop").offset().top},"1500"); return false})
      })
    </script>

    <% unless flash.empty? %>
      <script type="text/javascript">
        jQuery(document).ready(function($) {
          <% flash.each do |key, value| %>
            new PNotify({
                text: '<%= value %>',
                addclass: 'notice-<%= key %>',
                animate: {
                  animate: true,
                  in_class: 'rotateInDownLeft',
                  out_class: 'rotateOutUpRight'
                },
                icon: false
            });
          <% end %>
        });
      </script>
    <% end %>
  </body>

  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</html>
